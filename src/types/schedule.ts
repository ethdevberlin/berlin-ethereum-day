export interface ScheduleSession {
  time: string;
  speaker: string | null;
  project: string | null;
  sessionType: string | null;
  track: string | null;
  title: string | null;
  abstract: string | null;
}
